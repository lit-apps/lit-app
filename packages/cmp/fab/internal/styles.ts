import { css } from 'lit';
const styles = css`
  .fab.extended {
    width: inherit;
    
  }
  .fab.extended.large{
    gap: 12px;
  }

  .large.extended .label {
    font-size: var(--font-size-large, 1.25rem);
    font-weight: var(--font-weight-semi-bold, 500);
  }
  
`;
export default styles; 